import SwiftUI

struct SessionListView: View {
    var body: some View {
        NavigationStack {
            Text("Session list placeholder")
                .navigationTitle("Sessions")
        }
    }
}
