In staff-web/components/admin/MenuItemForm.tsx:

1. Add this import:

import MenuImageField from "./MenuImageField";

2. Replace the current Image URL label:

<label style={{ display: "grid", gap: 6 }}>
  <span>Image URL</span>
  <input value={imageUrl} onChange={(e) => setImageUrl(e.target.value)} />
</label>

with:

<MenuImageField
  value={imageUrl}
  onChange={setImageUrl}
/>
